name: Caller Workflow

on:
  push:
    branches:
      - main

jobs:
  use-reusable:
    uses: owner/repo/.github/workflows/feature-branch.yaml@main
    with:
      input-name: value
    secrets:
      SECRET_NAME: ${{ secrets.SECRET_NAME }}
